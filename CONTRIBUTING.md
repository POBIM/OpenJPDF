# ข้อกำหนดการมีส่วนร่วม (CONTRIBUTING GUIDE)

ขอบคุณที่สนใจที่จะมีส่วนร่วมในโปรเจค OpenJPDF! 🙏

## วิธีช่วยเหลือ

### รายงาน Bug 🐛

1. ไปที่ [Issues](../../issues)
2. คลิก "New Issue" 
3. เลือก "Bug Report"
4. อธิบายปัญหา:
   - ใช้ Windows เวอร์ชันอะไร?
   - OpenJPDF เวอร์ชันอะไร?
   - ขั้นตอนในการปรากฏ Bug
   - ผลที่คาดหวัง vs ผลจริง

### เสนอคุณสมบัติใหม่ 💡

1. ไปที่ [Issues](../../issues)
2. คลิก "New Issue"
3. เลือก "Feature Request"
4. อธิบาย:
   - คุณสมบัติคืออะไร?
   - ทำไมถึงมีประโยชน์?
   - ตัวอย่างการใช้งาน

### ปรับปรุงเอกสาร 📚

1. Fork repository
2. แก้ไขไฟล์ (README.md, เอกสารอื่นๆ)
3. Push ไป GitHub
4. สร้าง Pull Request

### แก้ไข Bug หรือเพิ่มคุณสมบัติ 🔧

**ข้อกำหนด:**
- Visual Studio 2022 หรือใหม่กว่า
- .NET 8.0 SDK
- Windows 10 หรือใหม่กว่า

**ขั้นตอน:**

```bash
# 1. Fork repository บน GitHub

# 2. Clone ของคุณ
git clone https://github.com/YOUR_USERNAME/OpenJPDF.git
cd OpenJPDF

# 3. สร้าง branch ใหม่
git checkout -b fix/issue-name
# หรือ
git checkout -b feature/feature-name

# 4. เปิดใน Visual Studio
# File > Open > Project/Solution > OpenJPDF.csproj

# 5. ทำการแก้ไข
# - ตรวจสอบ Build: Ctrl+Shift+B
# - ทดสอบ: F5 (Debug) หรือ Ctrl+F5 (Release)

# 6. Commit การเปลี่ยนแปลง
git add .
git commit -m "Fix: อธิบายสั้นๆ ว่าแก้อะไร"
# ตัวอย่าง:
# git commit -m "Fix: แก้ Bug OCR ไม่ทำงานกับภาษาไทย"
# git commit -m "Feature: เพิ่มการรองรับไฟล์ TIFF"

# 7. Push ไป GitHub
git push origin fix/issue-name

# 8. สร้าง Pull Request บน GitHub
# อธิบาย:
# - ทำการแก้ไขอะไร?
# - เหตุผล?
# - ทดสอบแล้วหรือยัง?
```

## มาตรฐานการเขียนโค้ด

### ตั้งชื่อ Classes

```csharp
// ✅ ถูก - CapitalCase
public class PdfService { }
public class OcrViewModel { }

// ❌ ผิด
public class pdfService { }
public class ocr_view_model { }
```

### ตั้งชื่อ Methods

```csharp
// ✅ ถูก - CapitalCase
public void SaveDocument() { }
public async Task<byte[]> ExtractTextAsync() { }

// ❌ ผิด
public void save_document() { }
public void extractText() { }
```

### ตั้งชื่อ Variables

```csharp
// ✅ ถูก - camelCase
private string fileName;
private int pageCount;
private List<Annotation> annotations;

// ❌ ผิด
private string FileName;
private int PageCount;
private List<Annotation> Annotations;
```

### Comments และ Documentation

```csharp
// ✅ ถูก
/// <summary>
/// บันทึกเอกสาร PDF ไปยังตำแหน่งที่ระบุ
/// </summary>
/// <param name="filePath">경로ไปยังไฟล์</param>
/// <returns>true หากสำเร็จ, false หากล้มเหลว</returns>
public bool SavePdf(string filePath)
{
    // โค้ด...
}

// ❌ ผิด
public bool SavePdf(string filePath)
{
    // save
}
```

### Null Checking

```csharp
// ✅ ถูก - Modern C# style
public void ProcessAnnotation(Annotation? annotation)
{
    if (annotation is null) return;
    // โค้ด...
}

// ✅ ยังถูก - Traditional style
public void ProcessAnnotation(Annotation annotation)
{
    if (annotation == null) return;
    // โค้ด...
}
```

## Process การ Review

1. **Automated Checks** (ถ้ามี)
   - Build ต้อง Pass
   - ไม่มี Compiler Warnings

2. **Code Review**
   - เจ้าของโปรเจค review โค้ด
   - อาจมีคำขอให้แก้ไข
   - Approve แล้ว Merge

3. **Merge**
   - Pull Request ถูก Merge ไปยัง main
   - ขอบคุณสำหรับการมีส่วนร่วม! 🎉

## ข้อปฏิบัติอื่นๆ

- ✅ ใช้ภาษาไทยหรืออังกฤษในโค้ด comments
- ✅ ทดสอบบน Windows ก่อน Submit
- ✅ อ่าน LICENSE ก่อนสมทบ (การเมืองใจ)
- ✅ ปฏิบัติตามกฎหมายและจริยธรรม
- ❌ ห้ามใช้งาน OpenJPDF เพื่อสิ่งที่ผิดกฎหมาย

## ติดต่อ

มีคำถาม? สามารถ:

- 📝 สร้าง Issue ใน GitHub
- 💬 แสดงความเห็นใน Pull Request
- 📧 ติดต่อผู้พัฒนา สิทธิชาติ โปธิสิงห์

---

ขอบคุณที่มีส่วนร่วม! 🙏
